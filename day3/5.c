// prime number check
#include<stdio.h>

int main(){

    /*
        A prime number can only be perfectly divided by 1 and the number itself (2 factors only)
    */
    int num;
    scanf("%d",&num);

    /*
        Check if the input number is a Prime number
        If not, print the square of the number
    */
    return 0;
}